/*
Encapsulation requires to define what properties and methods are private and
what methods/properties are public. For example, a car's engine is private
while its blinkers are public.

Encapsulation adds SECURITY. Properties and methods can be made private,
so they are not accessible outside the class.

Encapsulation hides COMPLEXITY. No one can see what's behind the object's curtain!

In JS, protected fields are prefixed with a _
*/
