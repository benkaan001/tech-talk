// return a new array with no duplicate characters

// not allowed to use new Set!
// return [...new Set(arr)]; // [ 'a', 'b' ]

array = ['a', 'b', 'a', 'a']; // expected result ['a','b']

const removeDupe = (arr) => {};

console.log(removeDupe(array));

console.log([1, 2, 3, 4].forEach((item) => console.log(item * 4)));
